<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.llbt.cms.dao.SystemParamMapper">
	<!-- 查询结果映射 -->
	<resultMap id="searchResultMap" type="com.llbt.cms.entity.SystemParam">
		<result property="id" column="id" />
		<result property="createBy" column="create_by" />
		<result property="createDate" column="create_date" />
		<result property="updateBy" column="update_by" />
		<result property="updateDate" column="update_date" />
		<result property="paramTitle" column="param_title" />
		<result property="paramKey" column="param_key" />
		<result property="paramValue" column="param_value" />
		<result property="paramDesc" column="param_desc" />
		<result property="isDelete" column="is_delete" />
	</resultMap>
	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
		id,create_by,create_date,update_by,update_date,param_title,param_key,param_value,param_desc,is_delete
	</sql>
	<!-- 新增记录 -->
	<insert id="insert" parameterType="com.llbt.cms.entity.SystemParam">
		INSERT INTO mms_system_param (
		id ,
		create_by ,
		create_date ,
		update_by ,
		update_date ,
		param_title ,
		param_key ,
		param_value ,
		param_desc ,
		is_delete
		) VALUES (
		#{id},
		#{createBy},
		#{createDate},
		#{updateBy},
		#{updateDate},
		#{paramTitle},
		#{paramKey},
		#{paramValue},
		#{paramDesc},
		#{isDelete}
		)
	</insert>
	<!-- 单记录删除 -->
	<delete id="delete" parameterType="java.lang.String">
		update mms_system_param set
		is_delete=1
		where id=#{id}
	</delete>
	<!-- 批量记录删除 -->
	<delete id="deleteByIds" parameterType="java.util.List">
		update mms_system_param set is_delete=1 where id in
		<foreach collection="list" item="ids" open="(" separator=","
			close=")">#{ids}</foreach>
	</delete>
	<!-- 单记录更新 -->
	<update id="update" parameterType="com.llbt.cms.entity.SystemParam">
		UPDATE mms_system_param
		<trim prefix="set" suffixOverrides=",">
			<if test="updateBy != null">
				update_by =#{updateBy},
			</if>
			<if test="updateDate != null">
				update_date =#{updateDate},
			</if>
			<if test="paramTitle != null">
				param_title =#{paramTitle},
			</if>
			<if test="paramKey != null">
				param_key =#{paramKey},
			</if>
			<if test="paramValue != null">
				param_value =#{paramValue},
			</if>
			<if test="paramDesc != null">
				param_desc =#{paramDesc},
			</if>
			<if test="isDelete != null">
				is_delete =#{isDelete},
			</if>
		</trim>
		where id=#{id}
	</update>
	<!-- 查询记录BY ID -->
	<select id="getById" resultMap="searchResultMap" parameterType="java.lang.String">
		select
		<include refid="columns" />
		from
		mms_system_param where id=#{id}
	</select>
	<!-- 查询所有记录 -->
	<select id="getAll" resultMap="searchResultMap" parameterType="java.lang.String">
		select
		<include refid="columns" />
		from
		mms_system_param where is_delete=0 order by create_date desc
	</select>
	<!-- 查询分页记录 -->
	<select id="queryPage" resultMap="searchResultMap"
		parameterType="com.llbt.cms.vo.Page">
			select 
		<include refid="columns" /> 
		from mms_system_param where is_delete=0 
		order by create_date desc 
		limit #{startLine},#{pageSize}
	</select>

	<select id="getSystemParamCount" resultType="int">
		select count(1) from
		mms_system_param where is_delete=0
	</select>
	<!-- 动态查询条件 -->
	<sql id="searchConditionSQL">
		<trim prefix="where" prefixOverrides="AND |OR ">
			<if test="id != null">
				and id = #{id}
			</if>
			<if test="createBy != null">
				and create_by = #{createBy}
			</if>
			<if test="createDate != null">
				and create_date = #{createDate}
			</if>
			<if test="updateBy != null">
				and update_by = #{updateBy}
			</if>
			<if test="updateDate != null">
				and update_date = #{updateDate}
			</if>
			<if test="paramTitle != null">
				and param_title = #{paramTitle}
			</if>
			<if test="paramKey != null">
				and param_key = #{paramKey}
			</if>
			<if test="paramValue != null">
				and param_value = #{paramValue}
			</if>
			<if test="paramDesc != null">
				and param_desc = #{paramDesc}
			</if>
			<if test="isDelete != null">
				and is_delete = #{isDelete}
			</if>
		</trim>
	</sql>

	<select id="getSysParamsMap" resultMap="searchResultMap">
		select
		<include refid="columns" />
		from
		mms_system_param where is_delete=0 order by create_date desc
	</select>


</mapper>